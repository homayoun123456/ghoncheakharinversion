# راهنمای مدیریت افزونه‌های وردپرس

## 🔌 قابلیت اتصال به مخزن وردپرس

سیستم مدیریت محتوای غنچه لاله‌زار اکنون قابلیت اتصال به مخزن رسمی افزونه‌های وردپرس (WordPress.org) را دارد.

---

## ✨ امکانات

### 1. جستجو در مخزن وردپرس
- جستجوی هزاران افزونه رایگان از مخزن رسمی وردپرس
- نمایش اطلاعات کامل افزونه‌ها شامل امتیاز، تعداد دانلود، نسخه و...
- دسته‌بندی افزونه‌ها (محبوب، پیشنهادی)

### 2. افزونه‌های پیشنهادی
- **Yoast SEO** - بهترین افزونه سئو برای وردپرس
- **Really Simple SSL** - فعال‌سازی آسان SSL/HTTPS
- **Contact Form 7** - فرم تماس حرفه‌ای
- **Elementor** - صفحه‌ساز پیشرفته
- **WooCommerce** - فروشگاه‌ساز
- **Wordfence** - امنیت وردپرس
- **Jetpack** - بهینه‌سازی و امنیت
- **Site Kit by Google** - ابزارهای گوگل

### 3. مدیریت افزونه‌ها
- نصب افزونه از مخزن وردپرس
- فعال/غیرفعال کردن افزونه‌ها
- حذف افزونه‌های نصب شده
- مشاهده جزئیات کامل افزونه

---

## 🚀 راه‌اندازی

### 1. اجرای سرور API
```bash
cd /workspace
npm start
```

سرور API روی پورت `3001` اجرا می‌شود.

### 2. دسترسی به پنل مدیریت
```
http://localhost:3001/admin/login.html
```

یا با سرو کردن فایل‌های استاتیک:
```bash
npx serve .
```

### 3. اطلاعات ورود
- **نام کاربری:** `admin`
- **رمز عبور:** `admin123`

---

## 📁 فایل‌های مرتبط

### Backend (API)
```
api-server.js    - سرور API با endpoint های مدیریت افزونه
```

### Frontend
```
admin/
├── login.html      - صفحه ورود
├── plugins.html    - صفحه مدیریت افزونه‌ها
├── plugins.css     - استایل‌های صفحه افزونه‌ها
├── plugins.js      - منطق JavaScript
└── index.html      - ریدایرکت به صفحه مناسب
```

---

## 🔧 API Endpoints

### افزونه‌های نصب شده
```
GET /api/plugins                    - لیست افزونه‌های نصب شده
```

### جستجو و مرور
```
GET /api/plugins/search?search=...  - جستجو در مخزن وردپرس
GET /api/plugins/popular            - افزونه‌های محبوب
GET /api/plugins/recommended        - افزونه‌های پیشنهادی
GET /api/plugins/info/:slug         - جزئیات افزونه
```

### مدیریت افزونه‌ها
```
POST /api/plugins/install           - نصب افزونه
POST /api/plugins/activate          - فعال‌سازی افزونه
POST /api/plugins/deactivate        - غیرفعال‌سازی افزونه
DELETE /api/plugins/:slug           - حذف افزونه
```

---

## 🎯 نمونه استفاده

### نصب Yoast SEO
1. وارد پنل مدیریت شوید
2. به صفحه "افزونه‌ها" بروید
3. روی تب "پیشنهادی" کلیک کنید
4. روی دکمه "نصب افزونه" کنار Yoast SEO کلیک کنید
5. پس از نصب، روی "فعال‌سازی" کلیک کنید

### نصب Really Simple SSL
1. به صفحه "افزونه‌ها" بروید
2. روی تب "پیشنهادی" کلیک کنید
3. کارت Really Simple SSL را پیدا کنید
4. روی "نصب افزونه" و سپس "فعال‌سازی" کلیک کنید

### جستجوی افزونه جدید
1. روی تب "جستجو در مخزن" کلیک کنید
2. نام افزونه را در کادر جستجو وارد کنید
3. Enter بزنید یا روی دکمه جستجو کلیک کنید
4. از نتایج، افزونه مورد نظر را نصب کنید

---

## 📝 نکات مهم

1. **اتصال اینترنت:** برای جستجو و نصب افزونه‌ها، اتصال اینترنت فعال لازم است.

2. **احراز هویت:** تمام عملیات‌های مدیریت افزونه‌ها نیاز به توکن JWT معتبر دارند.

3. **توسعه:** این سیستم برای توسعه است. در محیط تولید، از دیتابیس واقعی استفاده کنید.

4. **افزونه‌های واقعی:** این سیستم اطلاعات افزونه‌ها را از WordPress.org دریافت می‌کند اما فایل‌های واقعی افزونه دانلود نمی‌شوند (شبیه‌سازی).

---

## 🎨 رابط کاربری

### ویژگی‌ها
- طراحی واکنش‌گرا (Responsive)
- پشتیبانی از زبان فارسی و راست به چپ
- انیمیشن‌های نرم و حرفه‌ای
- اعلان‌های Toast برای بازخورد
- Modal برای نمایش جزئیات افزونه
- جستجوی سریع با دکمه‌های میانبر

---

## 🔒 امنیت

- تمام endpoint ها نیاز به احراز هویت دارند
- استفاده از JWT برای مدیریت session
- اعتبارسنجی ورودی‌ها

---

## 📞 پشتیبانی

در صورت بروز مشکل، فایل‌های زیر را بررسی کنید:
- کنسول مرورگر (F12)
- خروجی سرور Node.js

---

**نسخه:** 1.0.0  
**تاریخ:** دسامبر 2025
