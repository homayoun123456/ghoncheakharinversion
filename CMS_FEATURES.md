# سیستم مدیریت محتوای غنچه لاله زار
## Ghoncheye Lalehzar CMS - WordPress-like Features

این سیستم مدیریت محتوا شامل تمامی امکانات موجود در وردپرس برای نوشتن مقالات و پست‌ها می‌باشد.

---

## 🚀 راه‌اندازی سریع

```bash
# نصب وابستگی‌ها
npm install

# اجرای سرور
npm start

# سرور API روی پورت 3001 اجرا می‌شود
# http://localhost:3001
```

**اطلاعات ورود به پنل مدیریت:**
- نام کاربری: `admin`
- رمز عبور: `admin123`
- آدرس پنل: `/admin/`

---

## 📝 امکانات مدیریت نوشته‌ها (Posts)

### ویرایشگر پیشرفته متن
- ✅ ویرایشگر WYSIWYG (آنچه می‌بینید همان است که دریافت می‌کنید)
- ✅ قالب‌بندی متن (Bold, Italic, Underline, Strikethrough)
- ✅ تراز متن (راست، چپ، وسط، Justify)
- ✅ سرتیترها (H1 تا H4)
- ✅ لیست‌های شماره‌دار و نشانه‌دار
- ✅ نقل قول (Blockquote)
- ✅ کد (Pre)
- ✅ درج لینک
- ✅ درج تصویر از کتابخانه رسانه
- ✅ درج ویدیو (YouTube, Vimeo)
- ✅ حالت کد HTML
- ✅ حالت تمام صفحه
- ✅ ذخیره خودکار (Autosave)

### وضعیت‌های نوشته
- ✅ پیش‌نویس (Draft)
- ✅ در انتظار بررسی (Pending)
- ✅ منتشر شده (Published)
- ✅ زباله‌دان (Trash)
- ✅ پیش‌نویس خودکار (Auto-draft)

### زمان‌بندی انتشار
- ✅ انتشار فوری
- ✅ انتشار زمان‌بندی شده (Scheduled)

### بازنگری‌ها (Revisions)
- ✅ ذخیره خودکار بازنگری‌ها
- ✅ مشاهده تاریخچه تغییرات
- ✅ بازیابی نسخه‌های قبلی

---

## 📁 دسته‌بندی‌ها (Categories)

- ✅ ایجاد دسته‌بندی جدید
- ✅ دسته‌بندی سلسله مراتبی (والد/فرزند)
- ✅ نامک (Slug) خودکار
- ✅ توضیحات دسته‌بندی
- ✅ شمارش نوشته‌های هر دسته
- ✅ ویرایش و حذف دسته‌بندی
- ✅ دسته‌بندی پیش‌فرض

---

## 🏷️ برچسب‌ها (Tags)

- ✅ ایجاد برچسب جدید
- ✅ جستجو در برچسب‌ها
- ✅ برچسب‌های پرکاربرد
- ✅ نامک (Slug) خودکار
- ✅ شمارش نوشته‌های هر برچسب
- ✅ ویرایش و حذف برچسب

---

## 🖼️ تصویر شاخص (Featured Image)

- ✅ انتخاب از کتابخانه رسانه
- ✅ آپلود تصویر جدید
- ✅ پیش‌نمایش تصویر
- ✅ حذف تصویر شاخص

---

## 📚 کتابخانه رسانه (Media Library)

- ✅ آپلود فایل (Drag & Drop)
- ✅ پشتیبانی از تصویر (JPEG, PNG, GIF, WebP, SVG)
- ✅ پشتیبانی از ویدیو (MP4, WebM, OGG)
- ✅ پشتیبانی از صوت (MP3, WAV, OGG)
- ✅ پشتیبانی از اسناد (PDF, DOC, DOCX, XLS, XLSX)
- ✅ متن جایگزین (Alt Text)
- ✅ عنوان و توضیحات
- ✅ فیلتر بر اساس نوع
- ✅ جستجو در فایل‌ها
- ✅ نمایش شبکه‌ای و لیستی
- ✅ حذف فایل

---

## 💬 دیدگاه‌ها (Comments)

- ✅ نمایش دیدگاه‌ها
- ✅ دیدگاه‌های تودرتو (Threading)
- ✅ تأیید/رد دیدگاه
- ✅ علامت‌گذاری به عنوان هرزنامه
- ✅ حذف دیدگاه
- ✅ مدیریت خودکار (Moderation)
- ✅ ارسال دیدگاه از فرانت‌اند
- ✅ پاسخ به دیدگاه

---

## 🔍 سئو (SEO)

- ✅ عنوان سئو
- ✅ توضیحات متا
- ✅ کلمات کلیدی
- ✅ شمارنده کاراکتر
- ✅ پیش‌نمایش گوگل

---

## 📄 صفحات (Pages)

- ✅ ایجاد صفحه جدید
- ✅ صفحات سلسله مراتبی
- ✅ قالب صفحه
- ✅ ترتیب منو
- ✅ ویرایش و حذف صفحه

---

## 👥 کاربران (Users)

- ✅ مشاهده لیست کاربران
- ✅ ایجاد کاربر جدید
- ✅ نقش‌های کاربری:
  - مدیرکل (Administrator)
  - ویراستار (Editor)
  - نویسنده (Author)
  - مشارکت‌کننده (Contributor)
  - عضو (Subscriber)
- ✅ شمارش نوشته‌های هر کاربر

---

## ⚙️ تنظیمات (Settings)

- ✅ عنوان سایت
- ✅ توضیح کوتاه
- ✅ تعداد نوشته در هر صفحه
- ✅ دسته‌بندی پیش‌فرض
- ✅ تنظیمات دیدگاه‌ها

---

## 🌐 API Endpoints

### احراز هویت
```
POST /api/auth/login      - ورود
GET  /api/auth/check      - بررسی وضعیت
POST /api/auth/logout     - خروج
```

### نوشته‌ها
```
GET    /api/posts         - لیست نوشته‌ها (با فیلتر و صفحه‌بندی)
GET    /api/posts/:id     - دریافت نوشته
POST   /api/posts         - ایجاد نوشته
PUT    /api/posts/:id     - ویرایش نوشته
DELETE /api/posts/:id     - حذف نوشته
POST   /api/posts/autosave - ذخیره خودکار
GET    /api/posts/:id/revisions - بازنگری‌ها
POST   /api/posts/:id/restore - بازیابی از زباله‌دان
```

### دسته‌بندی‌ها
```
GET    /api/categories     - لیست دسته‌بندی‌ها
GET    /api/categories/:id - دریافت دسته‌بندی
POST   /api/categories     - ایجاد دسته‌بندی
PUT    /api/categories/:id - ویرایش دسته‌بندی
DELETE /api/categories/:id - حذف دسته‌بندی
```

### برچسب‌ها
```
GET    /api/tags          - لیست برچسب‌ها
GET    /api/tags/:id      - دریافت برچسب
POST   /api/tags          - ایجاد برچسب
PUT    /api/tags/:id      - ویرایش برچسب
DELETE /api/tags/:id      - حذف برچسب
```

### دیدگاه‌ها
```
GET    /api/comments      - لیست دیدگاه‌ها
GET    /api/posts/:id/comments - دیدگاه‌های یک نوشته
POST   /api/comments      - ارسال دیدگاه
PUT    /api/comments/:id  - ویرایش/تأیید دیدگاه
DELETE /api/comments/:id  - حذف دیدگاه
```

### رسانه
```
GET    /api/media         - لیست رسانه‌ها
GET    /api/media/:id     - دریافت رسانه
POST   /api/media/upload  - آپلود فایل
PUT    /api/media/:id     - ویرایش اطلاعات
DELETE /api/media/:id     - حذف فایل
```

### جستجو
```
GET /api/search?q=...     - جستجوی سراسری
```

### آمار
```
GET /api/stats/dashboard  - آمار داشبورد
```

### تنظیمات
```
GET /api/options          - دریافت تنظیمات
PUT /api/options          - ذخیره تنظیمات
```

---

## 📂 ساختار فایل‌ها

```
/
├── cms-api-server.js     # سرور API اصلی
├── database.json         # دیتابیس JSON
├── package.json          # تنظیمات npm
│
├── admin/                # پنل مدیریت
│   ├── index.html        # صفحه اصلی پنل
│   ├── admin-styles.css  # استایل پنل
│   └── admin-app.js      # اپلیکیشن JavaScript
│
├── blog.html             # صفحه وبلاگ
├── post.html             # صفحه نمایش نوشته
├── blog-styles.css       # استایل وبلاگ
├── blog-app.js           # اپلیکیشن وبلاگ
│
├── uploads/              # فایل‌های آپلود شده
│
└── index.html            # صفحه اصلی سایت
```

---

## 🔒 امنیت

- ✅ احراز هویت JWT
- ✅ هش رمز عبور با bcrypt
- ✅ محدودیت حجم فایل آپلود (50MB)
- ✅ فیلتر نوع فایل مجاز
- ✅ CORS پیکربندی شده

---

## 📱 طراحی واکنش‌گرا

- ✅ پشتیبانی از موبایل
- ✅ پشتیبانی از تبلت
- ✅ پشتیبانی از دسکتاپ
- ✅ منوی کناری قابل جمع شدن

---

## 🌍 پشتیبانی از زبان فارسی

- ✅ رابط کاربری فارسی
- ✅ پشتیبانی از RTL
- ✅ نمایش تاریخ شمسی
- ✅ اعداد فارسی

---

## 📝 نکات توسعه‌دهندگان

### افزودن فیلد جدید به نوشته‌ها

1. فیلد را به مدل `posts` در `database.json` اضافه کنید
2. API را در `cms-api-server.js` آپدیت کنید
3. فرم ادیتور را در `admin/index.html` آپدیت کنید
4. منطق ذخیره را در `admin/admin-app.js` آپدیت کنید

### افزودن نوع فایل جدید

در فایل `cms-api-server.js`، آرایه `allowedTypes` را در تابع `fileFilter` آپدیت کنید.

---

## 🆘 پشتیبانی

برای سوالات و پیشنهادات با ما تماس بگیرید.

---

ساخته شده با ❤️ برای غنچه لاله زار
